📘 ОТЧЁТ ПО ЛАБОРАТОРНОЙ РАБОТЕ: Анализ простых алгоритмов сортировки

1. Цель работы

Реализовать и экспериментально проанализировать четыре классических алгоритма сортировки:
	•	пузырьковая сортировка (bubble sort),
	•	шейкерная сортировка (cocktail shaker sort),
	•	сортировка вставками (insertion sort),
	•	сортировка выбором (selection sort).

Для каждого алгоритма измеряются:
	•	время выполнения,
	•	количество сравнений,
	•	количество перемещений (обменов/сдвигов),
	•	устойчивость,
	•	адаптивность,
	•	асимптотика в лучшем/среднем/худшем случаях.

⸻

2. Реализованные алгоритмы

Все алгоритмы реализованы in-place и возвращают статистику:

(comparisons, moves)

Устойчивость

Алгоритм	Устойчивый
Bubble Sort	✅ Да
Shaker Sort	⚠ Почти, но не гарантируется
Insertion Sort	✅ Да
Selection Sort	❌ Нет


⸻

3. Методика эксперимента

Для каждого алгоритма проводились тесты на массивах:
	•	отсортированный (best-case)
	•	обратно отсортированный (worst-case)
	•	случайный (average-case)

Размеры массивов:
	•	1000
	•	5000
	•	10000

Замеры времени выполнялись через time.perf_counter() 5 раз, выбиралась медиана.

Окружение:
	•	Python 3
	•	macOS
	•	CPU: Apple Silicon

⸻

4. Результаты эксперимента

Ниже приведены ключевые наблюдения на основе полученных данных.

⸻

⭐ Bubble Sort
	•	Лучший случай: O(n) — ранний выход, очень мало сравнений
	•	Худший/средний: O(n²)
	•	Очень много перемещений на случайных и обратных массивах
	•	Самый адаптивный среди всех простых сортировок

Вывод:

Отличен для почти отсортированных массивов, плох для больших и случайных данных.

⸻

⭐ Shaker Sort
	•	Два направления прохода
	•	По сравнению с bubble сортирует немного быстрее, но количество сравнений всегда одинаково
	•	Лучший случай — не O(n), потому что полный проход обязателен

Вывод:

Бесполезен по сравнению с bubble — те же O(n²), но хуже адаптивность.

⸻

⭐ Insertion Sort
	•	Лучший случай: O(n) — только один проход и минимум сравнений
	•	Худший: O(n²)
	•	Отличная адаптивность
	•	Устойчивый
	•	На случайных данных работает существенно быстрее bubble/shaker

Вывод:

Лучшая сортировка из всех четырёх на практике.

⸻

⭐ Selection Sort
	•	Количество сравнений всегда фиксировано: n·(n–1)/2
	•	Перемещений мало: максимум n
	•	Худшая устойчивость
	•	Почти не зависит от типа входа

Вывод:

Хорош для минимизации перемещений (например, при сортировке на флэш-памяти).

⸻

5. Сводные выводы по данным эксперимента

📌 1. Лучший случай (отсортированный массив)

Алгоритм	Время	Особенности
Bubble	🔥 мгновенно	ранний выход → O(n)
Shaker	❌ очень медленно	всегда полный проход
Insertion	🔥 почти мгновенно	только n сравнений
Selection	❌ медленно	сравнения всегда O(n²)


⸻

📌 2. Худший случай (обратный порядок)

Алгоритм	Время	Перемещения
Bubble	очень плохо	максимум
Shaker	как bubble	максимум
Insertion	лучше bubble	много сдвигов
Selection	стабильное время	перемещений всего n


⸻

📌 3. Средний случай (random)

Алгоритм	Итог
Bubble	самый слабый
Shaker	≈ bubble
Insertion	самый быстрый из всех
Selection	медленно, но стабильно


⸻

6. Асимптотический анализ

Алгоритм	Лучший	Средний	Худший	Устойчивость	Адаптивность
Bubble	O(n)	O(n²)	O(n²)	Да	Высокая
Shaker	O(n²)	O(n²)	O(n²)	Нет	Низкая
Insertion	O(n)	O(n²)	O(n²)	Да	Высокая
Selection	O(n²)	O(n²)	O(n²)	Нет	Нет


⸻

7. Итоговые выводы
	1.	Insertion Sort — лучший алгоритм среди исследуемых: устойчивый, адаптивный, быстрый в среднем.
	2.	Bubble Sort эффективен только для почти отсортированных массивов.
	3.	Shaker Sort не даёт реальных преимуществ над bubble.
	4.	Selection Sort стабильно медленный, но с минимальным числом перемещений — полезен для систем с дорогими операциями записи.

⸻